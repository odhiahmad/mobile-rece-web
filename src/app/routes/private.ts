import PageHome from 'app/components/pages/home/loadable';

const routes = [
  {
    key: 'page-home',
    name: 'page-home',
    component: PageHome,
    path: '/home',
  },
];

export default routes;
