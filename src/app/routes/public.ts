import PageLogin from 'app/components/pages/login/loadable';

const routes = [
  {
    key: 'page-login',
    name: 'page-login',
    component: PageLogin,
    path: '/',
  },
];

export default routes;
